<% include partials/header.ejs %>
    <div class="table-wrapper">
        <% if (players.length > 0) {%>
            <table class="table table-hovered text-center">
                <thead class="thead-dark">
                    <tr>
                        <th scope="col">ID</th>
                        <th scope="col">Foto</th>
                        <th scope="col">Nama Depan</th>
                        <th scope="col">Nama Belakang</th>
                        <th scope="col">Usia</th>
                        <th scope="col">Tinggi Badan</th>
                        <th scope="col">Berat Badan</th>
                        <th scope="col">Posisi</th>
                        <th scope="col">Nomor Punggung</th>
                        <th scope="col">Username</th>
                        <th scope="col">Aksi</th>
                    </tr>
                </thead>
                <tbody>
                    <% players.forEach((player, index) => { %>
                        <tr>
                            <th class="align-middle" scope="row"><%= player.id %></th>
                            <td class="align-middle"><img src="/assets/img/<%= player.image %>" class="rounded-circle player-img" alt=""></td>
                            <td class="align-middle"><%= player.first_name %></td>
                            <td class="align-middle"><%= player.last_name %></td>
                            <td class="align-middle"><%= player.age %></td>
                            <td class="align-middle"><%= player.height %> cm</td>
                            <td class="align-middle"><%= player.weight %> kg</td>
                            <td class="align-middle"><%= player.position %></td>
                            <td class="align-middle"><%= player.number %></td>
                            <td class="align-middle">@<%= player.user_name %></td>
                            <td class="align-middle">
                                <a href="/edit/<%= player.id %>" class="btn btn-sm btn-success">Ubah</a>
                                <a href="/delete/<%= player.id %>" class="btn btn-sm btn-danger">Hapus</a>
                            </td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        <% } else { %>
            <p class="text-center">Tidak ada pemain yang ditemukan. Tekan <a href="/add" >di sini</a> untuk menambah pemain.</p>
        <% } %>
    </div>
</div>
</body>
</html>